import React, { Dispatch, SetStateAction } from 'react'

function EditChannel({ setIsEditing } : { setIsEditing : Dispatch<SetStateAction<boolean>> }) {
  return (
    <div>EditChannel</div>
  )
}

export default EditChannel